{
  "name": "chess",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun src/index.ts",
    "compile": "bun build src/index.ts --compile --outfile=dist/chess",
    "bundle": "bun build src/index.ts --bundle --minify --outfile=dist/index.js"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^20.11.20"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "inquirer": "^9.2.15",
    "ora": "^8.0.1"
  }
}